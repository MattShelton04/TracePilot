{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$comment": "TracePilot performance budgets — CI enforced. Update when architecture changes intentionally increase a budget.",
  "frontend": {
    "totalBundleSizeKb": 2500,
    "largestChunkKb": 250,
    "initialLoadChunks": 3,
    "notes": "Measured from `pnpm --filter @tracepilot/desktop build --mode analyze`. Total includes JS + CSS assets."
  },
  "ipc": {
    "listSessionsMs": 200,
    "getSessionDetailMs": 100,
    "getSessionTurnsMs": 300,
    "searchContentMs": 500,
    "reindexSessionsMs": 30000,
    "getSearchFacetsMs": 500,
    "checkSystemDepsMs": 200,
    "getShutdownMetricsMs": 1000,
    "getWorktreeDiskUsageMs": 100,
    "getAnalyticsMs": 300,
    "getToolAnalysisMs": 300,
    "getCodeImpactMs": 300,
    "ftsHealthMs": 200,
    "notes": "Measured on a corpus of ~100 sessions. These are P95 targets. Cached IPC calls (checkSystemDeps, getWorktreeDiskUsage, getSearchFacets) assume warm cache."
  },
  "render": {
    "sessionListViewMs": 120,
    "chatViewModeMs": 200,
    "analyticsDashboardViewMs": 180,
    "orchestrationHomeViewMs": 150,
    "notes": "Time from component `onMounted` to the second `requestAnimationFrame` callback (first paint commit). Dev-only `useRenderBudget` composable in `apps/desktop/src/composables/useRenderBudget.ts` emits a `console.warn` when the measured delta exceeds the budget. Production builds compile out all instrumentation via `import.meta.env.DEV`."
  },
  "rust": {
    "parseEventsJsonl100kbNs": 5000000,
    "computeAnalytics100sessionsNs": 10000000,
    "searchContent100sessionsMs": 200,
    "notes": "Measured via `cargo bench -p tracepilot-bench`. Values are P95 of mean estimate."
  }
}
