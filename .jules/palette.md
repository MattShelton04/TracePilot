## 2024-05-19 - Semantic Pagination
**Learning:** Custom pagination components must be enclosed in a `<nav aria-label="Pagination">` wrapper instead of a simple `<div>`, use `aria-current="page"` on the active item, and hide decorative characters with `aria-hidden="true"`.
**Action:** Always implement this semantic structure when building or updating pagination components to ensure screen reader compatibility.
