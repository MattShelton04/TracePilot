# Validation and commit checkpoint

Final local frontend validation passes at `5ceece39`: **386 files / 3,497 tests**, with the scoped typecheck/build/policy results below. This 12 September 2026 record preserves earlier failures, complete retries, targeted checks and historical hosted results separately. The owner recorded evidence in ignored logs; raw logs, private session content, credentials and machine-specific paths are excluded. Passing automation does not complete every native state in the coverage matrix.

## Recorded validation

| Check | Recorded result | Scope |
| --- | --- | --- |
| Final clean full frontend retry at `5ceece39` | 386 files / 3,497 tests passed; exit zero | Desktop 277 / 2,269; UI 92 / 1,056; Types 6 / 115; Client 4 / 13; CLI 7 / 44. Complete retry after the test-only context-import correction; assertions/timeout unchanged. Details and failed attempt preserved below |
| `pnpm typecheck` | Passed all five packages with typecheck scripts | Snapshot before later audit edits |
| Earlier `pnpm test` completed retry | 365 files / 3,306 tests passed | Desktop 258 files / 2,090 tests; UI 90 / 1,044; Types 6 / 115; Client 4 / 13; CLI 7 / 44 |
| Fresh full frontend `pnpm test` | 375 files / 3,403 tests passed, zero failing summaries | Completed `full-frontend-latest.log`: Desktop 268 files / 2,187 tests; UI 90 / 1,044; Types 6 / 115; Client 4 / 13; CLI 7 / 44. Desktop duration 126.83 s; does not cover edits made after that run |
| Later complete frontend/typecheck/build checkpoint | 385 files / 3,491 tests, all five typecheck packages and build15.35 seconds passed | Completed before final UX-057 re-entry/scope follow-up. Its affected77 Config tests/typecheck are recorded separately; do not sum targeted cases into this full-suite count |
| `pnpm build` | Passed; Vite production build 25.37 seconds | Frontend production build; does not replace native workflow verification |
| `cargo test --workspace --exclude tracepilot-desktop` | 1,431 passed, 0 failed, 5 ignored across 24 groups, including 11 passing doctests | Desktop crate excluded; the native running app is tested separately |
| `cargo clippy --workspace --exclude tracepilot-desktop --all-targets -- -D warnings` | Passed | Same desktop-crate exclusion |
| `cargo fmt --all -- --check` | Passed | Rust formatting |
| `pnpm test:automation` | 6 passed, 0 failed, 0 skipped | Isolated automation fixtures; no claim of full product/native journey coverage |
| Visual report and extractor policy tests | 4 Node / 2 Python tests passed | Gallery generation and bounded artifact handling |
| Workflow action pins with remote commit verification | 35 references passed | Action pin policy |
| Changed-file Biome | 96 files passed, no errors/warnings; one informational suggestion | Relevant changed files; full repository advisory output is separately limited below |
| Version, file-size and documentation gates | Versions agree at 0.8.1; file sizes passed with zero allow-listed violations; tracked links 100 files and then-current audit links 5 files passed | Document edits after that snapshot need their own link/structure checks |
| Repository policy gates | ADR 14 passed; catalog drift and CSP passed; design emoji/backdrop checks passed for 240/273 files | Existing dependency/hoist advisories noted below |
| Later JSON layout regression | 3 real-Chromium geometry tests reproduced narrow keys before and passed after the shared fix | UX-031; native Explorer JSON D/M/L after images inspected, M key below viewport. Native actual CLI capture after D/M/L now verifies the shared fix; the original route/data pair differs |
| Later Settings numeric validation | Assigned owner reports 56 focused Settings/preference tests passed | UX-032/033; native invalid-width recovery, invalid Add prevention, valid 1.25 persistence, invalid existing draft/Escape and default reset checked in isolated profile; inspected after images. Remaining fields/bounds are separate |
| Later search feedback and maintenance locking | UX-034's three result cases failed before; original 4 tests passed after. Expanded DataStorage suite reports 18 passing tests, including real confirmation focus regressions | UX-034/036; native M rebuild result, both Rebuild controls disabled during path save, subsequent analytics count 74 and Reset Cancel/Escape focus return verified. Snapshot deletion untested at zero snapshots |
| Later setup navigation guards | 18 wizard/navigation tests, desktop typecheck and file-size policy passed | UX-035; native M invalid home blocks Continue/dots/heading ArrowRight; Skip saves valid startup home. Forced save failures/async races and missing default home retain component-only coverage |
| Later skill asset keyboard controls | 6 focused tests passed, including composition-event suppression | UX-037; native M named Open/Enter loads actual text, preview Escape restores Open, focused Remove visible. Physical IME and remaining New File cancellation cases untested |
| Later add-only native viewer fixture generation | 3 Node tests passed; Biome and syntax checks passed | Eight assets created, repeat created none and preserved eight; SHA256 of original manifest and rich-session files unchanged. Selected native Explorer D JSONL/CSV/invalid JSON/PNG/binary workflows now checked separately; generator tests alone do not prove viewer behavior |
| Native dialog gateway helper | Assigned owner reports 14 Node tests passed | Picker-result substitution only; native synthetic export/load now passes Rust original-archive validation/preview after UX-041 correction, then canceled unchanged. No successful import round trip or actual OS dialog is claimed |
| Later Analytics metric layout | 32 focused tests and 3 Chromium component layout tests passed | UX-039; native D/M/L geometry/content checked with no overflow; D/M/L after images inspected |
| Later Code Impact count labels | 13 targeted tests passed, including 2 new failing-before regressions; desktop typecheck, Biome and diff checks passed | UX-040; native D/M/L truthful units and no overflow; D/M/L after images inspected |
| Later full typecheck | All 5 package typechecks passed | Latest root run before date/Replay changes; does not cover subsequent edits |
| Replay keyboard | 52 tests passed, including 13 new cases; 9 failed before | UX-043 native after single-step/boundary/speed/Play/Pause checks pass; D/M/L no overflow. M/L after inspected; D rapid-seek scroll image is excluded; settled D image explicitly deferred to keep Replay OFF under latest user direction. Deeper Replay user-deferred |
| Later date-order validation | 12 component tests and 45 combined filter/analytics/page tests passed; desktop typecheck, Biome, diff check and independent review passed | UX-042 nine of eleven cases failed before. Paired drafts preserve last valid store/filter and block invalid fetches. Native M inline validation/correction and dates/repository persistence Analytics→Tools pass; matching after inspected. Physical segmented badInput untested; informational render-budget warning under concurrent typecheck |
| Later New Skill recovery/names | Three focused component tests and independent review passed | UX-044 native M inline duplicate alert after inspected; corrected name/description persist in 100-byte SKILL.md and editor fields. Other create variants and D/L remain separate |
| Tool Analysis populated checkpoint | Actual native All Time at D/M/L: 286 calls, 5 tools, 99.7% success, 1.1s average; `view` tool 281 calls/1 failure | No document overflow at all three sizes. D/M/L images inspected; tool controls/filter states remain untested |
| Later Import integrity | Native own-export hash failure reproduced at D; root inspected before image | 165 export Rust tests, Clippy and independent review pass. Same original 49,823B archive validates with Rust preview natively at D; after inspected and Import canceled unchanged. Deeper Import/custom-table reconstruction deferred |
| Later visual CI head-failure policy | 5 policy tests passed; publisher queue maximum added | Commit e1b2f2f4; hosted capture verified below, while Pages/comment delivery retains the default-branch bootstrap limitation |
| First GitHub-hosted visual capture | All four jobs passed; 33 cases per revision, 66 PNG dimensions/hashes verified; 24 changed / 9 identical / 0 incomplete / 0 base unavailable | Run 34684266923 at merge base 08b4e5e1 versus exact head fb2ac5eb. No recorded console/page errors or missing fixtures. About 74 s hosted wall time; base shards 20.654/23.960 s, head 18.301/22.779 s. Five Node and two extraction tests passed per runner |
| Analytics/Code commit checkpoint | 11 focused analytics, Code and sanitized export-picker tests passed | Commit 268b8fed; complements earlier32/13/3 layout counts, not a replacement full-suite run |
| Asset overwrite prevention follow-up | Initial Rust regressions reproduce former data loss; first exclusive-create revision passes native duplicate/error/disk-preservation check | UX-038; revised sibling staging/publish passes 26 Windows Rust tests, package Clippy with warnings denied, formatting and diff checks. Reader/writer failure, retry, collision cleanup and read-only permission preservation covered; Unix executable/symlink tests not executed on Windows. Final native D staging-backend duplicate/preservation/Open/Escape pass; D/M images inspected; M shows preserved 53-byte content/Open focus after closing preview/error, so it is not a matching duplicate-error pair |

The initial `pnpm test` attempt ran concurrently with typecheck and failed the unchanged syntax-highlighting Rust ReDoS timing smoke test: 231.216ms against a 200ms threshold. A complete retry without concurrent typecheck passed all packages. No threshold or implementation change was made to obtain that retry result. The 3,306 count belongs to that completed run, not an estimate of the later test inventory.

Targeted checks and native before/after results are attached to individual [findings](findings.md) and [surface rows](surface-coverage.md). Passing component mocks or browser geometry tests does not establish Rust IPC, persisted behavior or OS-dialog completion. Hosted Linux capture is now verified; automatic Pages/history/comment delivery remains unverified for the bootstrap reason recorded in [visual-regression.md](../../visual-regression.md).

## Baseline and tool limitations

- The public API baseline check fails because the existing `pub mod context_capture` on the default branch is absent from the stored baseline; the audit did not change that declaration relative to the default branch.
- Full repository design checks report two existing hexadecimal fallbacks in `skills-manager.css` and an existing literal z-index in `FileContextMenu.vue`, both unchanged against the default branch. Spacing-grid remains advisory with 648 reported values.
- Full-repository advisory Biome includes pre-existing generated artifacts with invalid JSON and reports errors/warnings. The relevant changed-file gate passes independently; it is not described as a clean full-repository lint result.
- The hosted production dependency audit reports two existing vulnerabilities, one high in `nanoid <3.3.18` and one moderate. The hosted Rust audit now reports 12 vulnerable dependency records plus 8 unmaintained and 8 unsound informational warnings. Both audit steps fail under existing `continue-on-error`, so the passing security job is not a clean dependency audit. Both lockfiles are unchanged from public base 08b4e5e1 at the reviewed PR head; dependency remediation remains separate from the usability changes. See the [security job](https://github.com/MattShelton04/TracePilot/actions/runs/34684266898/job/103528366462).
- Catalog drift passes with existing Vue peer-dependency and lucide hoist advisories. Five ignored Rust tests comprise the subagent-heavy reconstruction performance test and four documentation examples; they are not counted as executed passes.
- Validation-only cleanup removed redundant comments to satisfy file-size checks and formatted affected files. It did not change product behavior.
- Automatic approval review initially rejected bulk optional-feature enabling because effects on an existing profile were uncertain. The live owner verified the complete audit-owner manifest and all configured paths in the isolated audit home, then an exact live-home guard and scoped justification were approved. At that earlier checkpoint, optional features were enabled only there and no SDK connection was attempted. Later SDK unused-loopback failure checks are recorded separately below; no provider generation occurred. OS notifications/taskbar flash were disabled before the in-app alert test. This resolved tooling limitation is not a product defect.
- The earlier Check Now result was lost to a Rust watcher restart. A later native M retry displays latest 0.8.1, with inspected `settings-update-result-M.png`. This records the app result, not independent GitHub release verification. Native picker substitution tests do not establish an OS dialog or successful round trip; the later native export/load attempt now passes original-archive validation/preview after UX-041 correction, canceled unchanged.

## Coherent implementation commits

These commits were observed on the dedicated audit branch. They group completed changes by user-visible outcome; a commit does not imply all surrounding feature states have native coverage.

| Commit | Outcome |
| --- | --- |
| [c1412e8e](https://github.com/MattShelton04/TracePilot/commit/c1412e8e31163e8c07454633f9d0d2c174083e8c) | Keep setup and session controls usable by keyboard |
| [0ed0b2f9](https://github.com/MattShelton04/TracePilot/commit/0ed0b2f989ab3ff48456c9cd1bae18a3a36998dd) | Preserve dialog focus and unsaved skill edits |
| [6dfcb79f](https://github.com/MattShelton04/TracePilot/commit/6dfcb79f4ee0bbec251b0a55b147e043bcd4267b) | Compare populated desktop frontend views across pull requests |
| [a2570bf9](https://github.com/MattShelton04/TracePilot/commit/a2570bf966f6651bb66d4ef20a8f8d9c3f4fbbac) | Keep tabs, Explorer panes and Todo details reachable |
| [10ca2d62](https://github.com/MattShelton04/TracePilot/commit/10ca2d6228c917f2cd640e38327851572fdb9e3f) | Clarify comparisons and keep export previews current |
| [45f1a3ff](https://github.com/MattShelton04/TracePilot/commit/45f1a3ffd4bd8d00606da4b5899ef9f59423b30f) | Align palette destinations and restore keyboard controls |
| [ae5aa01a](https://github.com/MattShelton04/TracePilot/commit/ae5aa01a61d3545b5bde292809f88caed8fd632e) | Make desktop forms and worktree rows readable |
| [278d7ea7](https://github.com/MattShelton04/TracePilot/commit/278d7ea7d8237c44b162ba8b9e568e6ee4c2540a) | Validate Settings drafts, guard setup navigation and serialize data maintenance |
| [e1b2f2f4](https://github.com/MattShelton04/TracePilot/commit/e1b2f2f4bcb251c28cb3f0bd66e1686859f2a3e9) | Make visual CI failures explicit and prepare isolated native viewer fixtures |
| [268b8fed](https://github.com/MattShelton04/TracePilot/commit/268b8fed389e62e3d780bbe5afaf6ad7560a87a7) | Keep analytics metrics readable and clarify file count units |

Latest bounded commits: [2200ee2c](https://github.com/MattShelton04/TracePilot/commit/2200ee2c) final Skills controls/recovery (25 focused tests); [d4d78a2e](https://github.com/MattShelton04/TracePilot/commit/d4d78a2e) original export integrity (165 tests); [2f49b7ba](https://github.com/MattShelton04/TracePilot/commit/2f49b7ba) paired date drafts (45 tests); [b40e4c20](https://github.com/MattShelton04/TracePilot/commit/b40e4c20) Replay keyboard ownership (52 tests). Independent Code/date review additionally reports 31 focused tests passed. These overlapping targeted groups are not summed into a full-suite count.

Checkpoint [fb2ac5eb](https://github.com/MattShelton04/TracePilot/commit/fb2ac5eb) was pushed and [draft PR #813](https://github.com/MattShelton04/TracePilot/pull/813) opened. Later changes, final review, remaining native reconciliation, evidence privacy review and final PR readiness remain part of the ongoing audit. Deeper MCP, Worktrees, Launcher, Import, Compare, Replay, SDK and Alerts workflows remain deferred at the user's direction; no exhaustive audit claim is made.

## Publication checkpoint

Nine unpublished audit commits were checked after sanitizing a private test title: no rewritten patch or current target test contains the removed token, only the intended test file differs in affected rewritten trees, and the real index remained empty during this review. Report commit links use the verified rewrite journal mapping. Six root-inspected fixture-only toolbar/Analytics/Code before/after copies are approved public evidence; private captures and raw logs remain ignored. This does not approve unrelated artifacts or claim the audit complete.

## Subsequent native route checkpoint

All 28 route rows have native visits; the124-row matrix now has97 Partial,0 Source-only and27 Deferred remainder rows. Earlier Config Injector empty/default, Not Found, Alerts/About, Search and retained-tab/help observations remain bounded. Later Config Injector M discovers two agents/two versions/two backups from owned config-only fixtures; UX-053/054 native after supplements the initial empty checks, with UX-057 ordinary restore/route re-entry now verified and the final guard independently reviewed. SDK UX-048/049 native M failure/accessibility and D/L controls pass. Actual Conversation frontend ErrorBoundary evidence moves G10 to Partial M; later native capture preflight/progress/error/cancel checks also move C10/C11 to Partial M, leaving no Source-only rows. At the user's later direction, deeper SDK/Alerts joins existing deferrals: G08/S09/P10/P11/P12 retain earlier checks under Deferred remainder. SDK/Replay/Alerts are OFF. No route visit or config marker is promoted to full workflow/CLI execution coverage.

Alerts keyboard read/dismiss/clear/reopen/Escape focus assertions passed with two test events; the D populated image caught animation, while M empty was settled and inspected. About showed 0.8.1/build b40e4c20/74 sessions/796KB; Check Now returned latest 0.8.1 in the app. Relevant images were inspected. Actual session-alert navigation, OS delivery and remaining About/update controls are unverified.

The fresh full frontend run completed successfully: **375 files / 3,403 tests**.
Its five package summaries all report passed and `Done`; there are no failing
test summaries or unhandled-error summaries. This is a separate completed run
from the earlier 3,306-test snapshot, not a sum of overlapping targeted tests.

## Hosted PR checkpoint

[PR #813](https://github.com/MattShelton04/TracePilot/pull/813) has **17 passing
reported checks** at published head `fb2ac5eb`, including the required CI
aggregate, frontend build/tests, Windows and Ubuntu Rust jobs, policy, CodeQL,
bundle analysis and all four visual-capture jobs. This status does not cover
later local commits. The passing aggregate includes advisory steps that failed:

- At published fb2ac5eb, frontend lint exits 1 for two `noNonNullAssertion` warnings in the PR-added
  `packages/ui/desktop-tests/AnalyticsMetricPanels.ct.ts:31–32`. The PR's
  `TabNav.vue:72` also has an informational `useIndexOf` suggestion; that is not
  a failing warning.
- At published fb2ac5eb, Rust formatting exits 1 for PR-added
  `crates/tracepilot-orchestrator/src/skills/assets/tests.rs:143` and
  `skills/assets/tests/atomic.rs:2,91`. The initial read-only formatting check reproduced those differences; subsequent local cleanup corrects them.
- Ubuntu Clippy exits 101 for eight diagnostics in six unchanged baseline
  files: `tracepilot-bench/src/events.rs` (checked division), and orchestrator
  `context_capture/persistence.rs`, `templates/storage.rs` (sorting),
  `process/encoding.rs` (ceiling division), `worktrees/ops.rs` (redundant borrow),
  `bridge/discovery.rs` (trim and nested conditions). All six files are unchanged
  from 08b4e5e1 in both the published head and the reviewed working tree. The
  hosted diagnostics use Rust 1.98.0; the earlier local Clippy pass does not
  override this hosted result.
- Rust and production dependency audit failures remain as described above.

The PR-added lint/format differences were corrected locally in the three affected files; targeted Biome and Rust formatting now pass. Hosted results at fb2ac5eb remain historical and do not prove a new hosted run passed. Baseline Clippy and dependency advisories remain explicit debt. No advisory setting or failure
threshold was changed to report these checks as passing. The
[hosted CI run](https://github.com/MattShelton04/TracePilot/actions/runs/34684266898)
and [visual run](https://github.com/MattShelton04/TracePilot/actions/runs/34684266923)
provide the checkpoint evidence. The downloaded visual artifacts produced a
complete local gallery, with representative image inspection documented in
[visual-regression.md](../../visual-regression.md); automatic publication and
PR-comment delivery remain untested until the publisher is merged to `main`.

## Latest grouped Search, keyboard-help and retained-tab checks

- UX-045: 55 focused/neighbor desktop tests and four Chromium geometry/native-key tests pass; desktop typecheck/Biome/diff pass. Native M header width/scroll 356/356 and Space/Enter collapse/expand pass. Final no-outer-tab D/M/L images inspected: headers836/836,356/356,1956/1956; titles788/308/791. Per-session Filter Enter→one match verified. Facets286before/73after differ, so no pixel-identical state pair.
- UX-046: three corrected Chromium cases fail before (560px scroll vs 520px client), then pass. Independent review corrected shared Vite alias/plugin preservation and L height. The final desktop CT run passes 10/10 (three Keyboard Help, three Analytics, four grouped Search), recorded in the ignored desktop-layout-final.log. Native D/M/L after images inspected: body 518/518, all keys inside padding, Ctrl+/ opens, Escape restores, editable ? suppressed. Initial alias/fragment-root harness attempts are not product evidence.
- UX-047: 13 new tests pass (12 failed before after fixing the Home-inclusive test selector), plus four store/seven drag tests =24. Typecheck/Biome/diff pass. Native M menu after focuses Close, supports Arrow/Home/End and Escape return. Pop Out Enter creates actual Rust child; D Overview and M Conversation load recorded data, Compact Space/Chat Enter/no-overflow checks pass. Menu after inspected but changed background prevents matching pair. Native Tab dismiss/Close Others named-rich focus/Close All sidebar Sessions pass. All 8 bounded child tabs, actual800×500 Explorer content/nooverflow and repeatPopOut/Closewindow pass; childConversation/minExplorer images inspected. Delete recovery unconfirmed (aria-label null without tag); edge/other child states remain untested.
- DEV-001: development HMR injection-key duplication is separated from product findings. Clean Vite restart restores 842 events/140 turns/67.62 AIC. Stable-key hardening in e42ccf64 has two failing-before regressions within 60 passing focused tests/typecheck. Native after verifies those Overview values and identical context keys imported through two query URLs. The root-inspected `retained-session-data-after-D.png` shows the correct Overview values; this is module-identity/render verification, not a scripted full HMR mutation. No packaged-runtime defect is inferred; UX-048 separately records the SDK diagnostic failure.

These later targeted results supplement the completed 375-file/3,403-test frontend checkpoint; they are not summed into a new full-suite count.

Latest bounded MCP native M after checks pass for UX-006/007/011/012: all Add/environment labels, Stdio/HTTP URL switching, removed unsupported controls, literal argument preview and two-argument save/reopen/disk equality, plus284px nonoverflowing2×2healthtiles/Response6ms. Three after images inspected. No healthy transport/service/credential lifecycle is claimed; deep MCP remains deferred.

UX-012 review follow-up: native M Description-only editing splits an embedded-newline argument in an owned fixture. The before image was inspected and the original two arguments were restored immediately. Preservation passes 32 focused tests/typecheck; native M after has readOnly true with associated guidance, and Description Save Enter retains the exact two-element array including the embedded newline in Rust-written JSON. `mcp-multiline-after-M.png` is root inspected. The full pre-test backup was restored and reload/Edit confirms ordinary comma/internal-space arguments have readOnly false. The final native CR/12px-helper check below also passes with inspected evidence and full backup hash restoration.

UX-048/049: native M SDK failure/field-access defects have inspected before images. Corrections pass 35 focused tests, independent 23/typecheck/Biome; overlapping groups are not summed. Native M failure against unused loopback65534 now logs only Connect failed/os10061, skips false-success lines and permits retry. Advanced Space/Enter and named log level/CLI URL pass; Warn→Info restored. The inspected failure-after differs in scroll/state/timestamps. Owned Stdio Connect/diagnostics succeeds with CLI 1.0.83/authentication/14models/zero tracked sessions; connected log level is disabled. D/L sdk-controls-after images are inspected with no document overflow/named disabled controls; private account content remains ignored. No linking/resumption/inference occurred; auto-connect preference timing remains an unvalidated lead. The owned empty bridge was disconnected and SDK/Replay/Enable alerts switched OFF; native allfalse, SDK/Replay TOMLfalse and original SDK settings-record absence are verified. Alerts TOML [alerts].enabled=false is now verified; deep SDK/Alerts work is user-deferred.

The following additional coherent commits completed locally after the published fb2ac5eb checkpoint: [7866ad9b](https://github.com/MattShelton04/TracePilot/commit/7866ad9b) grouped Search readability and keyboard collapse; [037c94d5](https://github.com/MattShelton04/TracePilot/commit/037c94d5) complete keyboard-help key display; [c2bcee13](https://github.com/MattShelton04/TracePilot/commit/c2bcee13) keyboard session-tab menus/closing; [e42ccf64](https://github.com/MattShelton04/TracePilot/commit/e42ccf64) stable session context across development reloads; [a4838f6d](https://github.com/MattShelton04/TracePilot/commit/a4838f6d) audit regression-check cleanup; [e253e858](https://github.com/MattShelton04/TracePilot/commit/e253e858) truthful SDK diagnostics and accessible controls. Their local existence does not claim newer hosted CI/publication than the recorded fb2ac5eb checkpoint.

Later Explorer native M name/content search→actual highlighted café line5 and no-match/Clear/Escape pass; result image inspected. UX-050 before has role-less menu/underlying focus/bottom672/Escape failure. Native after now focuses Copy File Path, supports End→Open Containing Folder/Home→first and Escape hidden/exact-file restoration; rect381,523,160×109 ends632 inside640. explorer-menu-after-M.png is inspected, with changed background/retained tabs preventing an exact pair. Combined 26 file/tab/model-picker tests, bounded independent review, targeted Biome/diff pass; D/L/file variants remain. No clipboard/external-folder action was invoked.

Twelve owned config-only fixtures populate native M Config Injector with two agents/two versions/two backups; markers are not executable CLI proof. UX-053/054 passes 66 tests/typecheck plus independent 33-test review. Native M after names/labels across all tabs and Show all tools Space expand/Enter close/focus pass. Boolean false→Space true enables Save/Diff2changes; Save Enter writes true, preserves auditPreserveMe/disabledSkills, reload reads true and false/Save restores. Owned Unicode-label backup creation2→3, Preview Enter actual-file diff/Close and two-step delete→2 pass; same-version Load Diffs disabled/distinct versions two diffs. config-agents/boolean/backups/versions-after-M.png are inspected, but backup image shows form/top list only, not offscreen full preview. Restore false backup after true Save writes diskfalse while UItrue/Save/diff stay stale for minutes: UX-057, config-restore-stale-before-M.png inspected. Native ordinary/route re-entry after now passes; final77 tests/typecheck/independent 41-test review pass. Exact manifest settings.json/config.json contents are restored with both hashes matching.

Unlinked SDK Conversation/model-picker before M is inspected: Link untouched, 14 models plus default and UX-052 focus/Escape/selection-semantics failure. Shared menu correction passes combined 26 tests/bounded independent review/Biome. The first native revisit hit DEV-001's real frontend missing-provider boundary; Try Again/reload failed while Settings navigation worked, in misnamed conversation-native-boundary-D.png at actual M960×640. After SDK/MCP stable-key hardening and clean native restart, both render normally with zero current console errors. Native M picker after has 15 menuitemradios, End Grok 4.6/Home Default/Escape exact default-trigger restoration; sdk-model-picker-populated-after-M.png is root inspected. No model selection/link/send/inference; bridge disconnected, SDK OFF, originally absent settings record restored. Full live HMR mutation remains unverified.

Native D SQLite audit_metrics120rows/four columns has inspected UX-051 before evidence. After Enter opens full Unicode and Escape restores the exact cell; Home→View name,row1/Right→value and note resize180→196ArrowRight/Enter180 pass, with one data tab stop. sqlite-cell-after-D.png is root inspected; retained-tab differences prevent an exact pair. Data/Schema Space/four metadata columns/no indexes remain verified. M/L keyboard/containment now also passes; remaining value/table/copy variants are separate. SVG Plain Text406B matches Rust's unknown-extension Text fallback; SVG image rendering is outside the current contract. No clipboard action was invoked.

UX-055/056 independently passes 55 tests (50 desktop/five shared resize), desktop typecheck, Biome10 files and diff review without actionable findings. Native M after minimum300px/415px, Home41.666%/Right43.888%/End57.638%/Enter50%; D600/595px/L1160/1155px/no outer overflow. skill-splitter-after-M.png, skill-markdown-after-M.png, skill-editor-after-D.png/L.png are inspected. H1 whole-first-line and Bullet final-Unicode-line Ctrl+S/disk/reload pass. Metadata hint/tools/automatic OFF persists. Owned0B reference-café.md settles to empty preview (initial async placeholder is not failure); Escape exact focus and Remove Cancel+confirm/disk deletion pass. Skill disable Space/reload unchecked/reenable→disabledSkills[], search no-match/reset/Project empty/All and Delete Cancel/Escape focus/confirm pass; five globals remain. No existing user skill changed; native undo/physical IME remain unverified.

UX-050 later D/L keyboard/pointer menu placement and Tab dismissal/exact-file focus pass with inspected images; binary M Copy Path/Open Folder/Escape passes; directory/actions remain. UX-051 later M/L Enter full Unicode/Escape exact cell, Ctrl+End row120last/Ctrl+Home row1first, resize196→180 reset, one data tab stop/no outer overflow pass with inspected images. Commit22a02025 contains SQLite keyboard controls; these native results do not imply clipboard coverage.

UX-057 initial correction passes native M false backup→true Save→Restore with UI/diskfalse, Save disabled/diff0. config-restore-stale-after-M.png is inspected, with different vertical scroll/toasts from before. Independent deferred-promise remount tests then fail twice: pending write leaves a new view stale; late superseded reread nulls a newer valid config. Per-mounted-editor clean-field synchronization and following the latest guarded initialization now pass 77 Config tests, including both failing-before cases and retained re-entry dirty edits; desktop typecheck passes. Native ordinary/route re-entry passes; final Restore-only scope guard passes independent 41-test review. The owned audit-restore-verification backup was deleted through two-step UI; baseline two backups remain.

UX-012 independent review found carriage-return arguments normalize in textarea values; the CR/LF preservation guard passes eight focused tests. Final native M `['first\rsecond', '--label=two words']` is readonly with associated 12px helper; Description-only Save preserves the exact two arguments on disk. mcp-cr-preservation-after-M.png is root inspected and the full original MCP backup hash is restored. UX-010 final native M unchanged audit-check editor also shows Saved with Save disabled, recorded in skill-saved-final.yml.

Native M capture: real CLI 1.0.83 preflight ready, three protocol choices/default OpenAI Responses/Save OFF. Capture request reaches visible nine-stage progress then a recoverable isolated CLI exited before sending a model request (exit1). capture-preflight-M.png, capture-progress-M.png and capture-error-M.png are root inspected/private. Stable failure was confirmed after an initial HMR interruption. Fresh retry/Cancel capture Enter reports Capture cancelled/restores editable form; Back to snapshots shows empty guidance. Fifteen source-file hashes remain unchanged, scratch0/saved0. C10/C11 are Partial M; later compatible-fixture native capture/save/reload/delete passes; earlier exit1 is retained as observed failure. With SDK OFF, core Conversation Chat/Compact/Timeline renders with zero boundaries; the later clean-runtime SDK-enabled UX-052 after also passes.

Successful capture checkpoint: a distinct compatible owned CLI 1.0.83 fixture produces68.1KB/7items/16tools/1system at native D. Overview/System/Items/Tools/Raw keyboard tabs pass. View once Back→empty; Save ON recapture→one saved→reload/open plaintext69,542chars. Raw mode explicitly labels its20k line limit. Delete Cancel/Escape restores focus; confirm returns empty/disk0. Save OFF restored,15 source hashes unchanged/scratch0/saved0; only the owner/path-checked temporary folder was removed, preserving original rich data. capture-success-overview-D.png and capture-json-tree-after-D.png/M.png/L.png are inspected/private. UX-031 actual CLI 35,849char value retains85.78px key/top alignment/wrapping/no outer overflow at D/M/L; original route/data pair differs. Compatible generator correction passes three tests.

Later native Settings/core checks: M Hide empty73→74→73; Markdown/Rich Rendering OFF persist into raw Chat/tool parameters (zero rendered children), ON restores one rendered child. D Light125% slider and L24-card repo/long-branch/Most-turns→Conversation→Sessions retained context images are inspected; Dark100% and tested preferences restored. Agent reviewer model autosave/disk/reload/restoration passes. Exact original settings.json/config.json manifest contents have matching hashes; Alerts TOMLfalse is verified. Additional local commits are22a02025 SQLite,9c757bd1 Skills and96a7399c shared menus. The3,491-test/385-file full suite, five-package typecheck and15.35s build predate final UX-057; affected77 Config tests/typecheck and independent 41-test review cover that final delta. No newer hosted CI result is claimed here.

The product source checkpoint is local commit `a8a4e3bc`; only these six audit documents remained uncommitted when the live owner reported it. Independent review also passed ten bounded HMR/MCP tests, including stable context-key regressions. Native clean-runtime UX-010/012/052 closeouts above belong to this product checkpoint. A later test-only stability commit is recorded below. None implies a full live HMR mutation or a newer hosted Actions result.

Final bounded native closeouts: UX-029 settled M palette after waits for loading to hide, then shows No results/zero Replay actions; the earlier loading image is rejected. UX-023 settled disabled/coming-soon image and Manage Worktrees Space route activation pass. UX-024 M owned registration has a 256px branch, seven aligned header/cell positions/widths, Space sort ascending→descending and Enter Rust Details; table/detail images inspected. Only that new registration is removed, returning zero rows/repositories with files untouched. UX-020 D query AUDIT FIXTURE has 72 options; ArrowDown/Enter selects rich fixture and loads a 49,369-character Rust preview, inspected. UX-022 aligned D/M mode headers verify JSON Raw true/Rendered false and Zip Raw true/Rendered disabled/no Copy/guidance. M Worktree and final Export data differ from original before images, so no matching pair is claimed. UX-043 settled D image is explicitly deferred to keep Replay OFF; earlier native D transport assertions and inspected M/L after images remain valid.

## Final full-suite retry

The preceding full frontend attempt failed one newly added context-module import test at its 5,000ms timeout; the other desktop results reported 276 files / 2,268 tests passing and the remaining packages passed. Test-only commit `5ceece39` loads the real helper leaves while retaining the same assertions and timeout; two focused tests, 75 adjacent tests, desktop typecheck and independent review pass. The clean full retry then exited zero: **386 files / 3,497 tests passed** in ignored full-frontend-final-verified.log. Desktop: 277 files / 2,269 tests (124.44s); UI: 92 / 1,056; Types: 6 / 115; Client: 4 / 13; CLI: 7 / 44. This is a complete retry, not a sum of overlapping targeted runs. The product build passed in 40.69 seconds before the final test-only edit; workspace typecheck and latest desktop typecheck pass. File-size policy passes with zero allow-listed violations. The earlier failed attempt remains recorded; it is not the latest result.

Final UX-004 native after passes on a separate ownership-marked empty profile: M guidance/Settings Enter and pointer/return/reload, D/M/L inspected images and zero outer overflow. The populated profile is restored with unchanged closed config.toml/index.db/repo-registry.json hashes and real Rust IPC startup. Export no-match/Escape preserves rich selection and pointer Recovery selection loads a real JSON preview. UX-019 final native D owned empty→Rich zero-baseline and Rich→Long whole-percent rounding now pass with inspected compare-zero-final-after-D.png/compare-rounding-final-after-D.png: turns2700%, duration2056%, zero-baseline turns/tools/duration/files/lines show From 0. Twenty calculation tests supplement native; final fixtures differ from original real data, so no exact pair. Deeper Compare remains deferred.

Final native app state is restored to the populated profile at D1440×960, Dark/100%, Sessions with 73 visible items and an empty search. SDK/Replay/Alerts TOML values are OFF. The current console reports zero errors and three warnings; no inference about the warnings' causes is made here. File-size policy passes with zero allow-listed violations.
