# TracePilot desktop usability audit — 12 September 2026

Status: scoped native audit reconciled with explicit limits; final local validation passed. [Draft PR #813](https://github.com/MattShelton04/TracePilot/pull/813) contains the published checkpoint, with the final update/review tracked separately. This is not a claim of exhaustive testing.

## Environment and safeguards

- Baseline `08b4e5e1`; dedicated branch `codex/desktop-usability-audit`; original working tree clean.
- Automation PR [#812](https://github.com/MattShelton04/TracePilot/pull/812) is merged and already included. Reuse its pinned Playwright CLI and `pnpm app:start` workflow.
- Primary environment: Windows Tauri/WebView2 with real Rust IPC. Viewports are CSS pixels: D = 1440×960, M = 960×640, L = 2560×1440.
- Root agent exclusively owns live app. Source inventory and isolation research are delegated without live app access.
- Existing user resources are read-only; mutations/destructive tests require disposable fixtures or an isolated profile. Captures containing real session content remain ignored/private.
- Current Settings/Skills mutation checks run in an isolated profile. Replay, Exact Capture, Config Injector, MCP and alerts were enabled there only after owner-manifest/live-home checks; OS notifications/taskbar flash were disabled for the alert test. Owned SDK metadata checks succeeded with CLI 1.0.83, authentication, 14 models and zero tracked sessions, without linking/resumption/inference. At the user's later direction, the owned empty bridge was disconnected and SDK/Replay/Enable alerts switched OFF at M. All three visible states are false; SDK/Replay TOML values are false and the originally absent SDK settings record was removed. Alerts TOML [alerts].enabled=false is now verified. CLI-startup changes were removed by restoring exact manifest settings.json/config.json contents; both hashes match. Invalid width was restored to 1600, disposable pricing entries removed, default rate restored from 99 to 5 and Warn logging restored to Info. The invalid wizard home and duplicate-asset fixture were restored. MCP exact embedded-newline and CR argument persistence pass; the associated 12px helper is inspected and the full original backup hash is restored. Ordinary arguments reload editable.
- Initial sandbox launch failed with Vite `spawn EPERM`; documented launcher retried with process permissions. This is an execution-environment limitation, not a product finding.

## Coverage matrix

`Untested` means no attributable check at that viewport. `Partial` covers only the named observations, not every state or variation. Deeper MCP, Worktrees, Launcher, Import, Compare, Replay, SDK and Alerts are deferred at the user's direction. Other unexercised variants are explicit limits of this audit, not completed checks or promises of a future continuation. The [source inventory](source-inventory.md) and [per-surface matrix](surface-coverage.md) reconcile 124 stable IDs; [findings](findings.md) distinguish implemented fixes, native evidence, automated regressions and remaining limits.

| Surface / workflow | States and interactions | D | M | L | Findings / verification / limitations |
| --- | --- | --- | --- | --- | --- |
| Bootstrap / setup / indexing | First run, folder validation, loading, completion, recovery | Partial | Partial | Partial | Real isolated first run, invalid/edit/reset, completion, 74-session indexing and factory reset exercised. UX-002/003 native rechecks passed. UX-035 native M invalid home disables Continue and Step 4/5; heading ArrowRight remains on Step 3. Skip saves the valid startup home, confirmed in Settings; after image inspected. OS picker, absent default-home behavior and forced save failure remain untested. |
| App shell | Sidebar expanded/collapsed, navigation, breadcrumbs, theme, scaling, shortcuts, search palette, alerts, updates | Partial | Partial | Partial | D/M/L Settings-shell smoke exercised expanded/collapsed navigation. UX-013 inner detail tabs reveal the active end tab without horizontal page movement at M; dark scrollbar inspected. UX-028 native M Enter on palette Clear empties query/restores input focus. UX-029 excludes disabled Replay and Ctrl+K → Skills → Enter visibly opens Skills. Final settled palette image is inspected after Loading results hides; No results and zero Replay actions are verified. Remaining destination/gate variants are separate. Ctrl-click three retained tabs and Home/End navigation checked. UX-047 native menu focus/navigation/Escape return and real Pop Out child pass; independent retained subtabs plus All sessions Space/tab Enter checked. UX-046 Keyboard Help D/M/L contains all keys; Ctrl+/ and Escape/focus restoration plus editable ? suppression pass. Detached windows, maximize/DPI and other shortcuts remain separate. |
| Sessions | Populated/empty, search, filters, sorting, view modes, selection, bulk actions, tabs/context menus | Partial | Partial | Partial | Existing 583-session list inspected D/M; fixture list exercised. UX-004 fresh owned empty profile now shows No sessions yet/new guidance at D/M/L without outer overflow; M Open Settings Enter/pointer and reload persistence pass, all after images inspected. UX-001 M visual and no-match Clear filters pass. Native L audit/demo + long branch + Most turns shows 24 cards; rich Conversation → Sessions preserves repository, branch, sort and search, with inspected evidence. Remaining filter/sort/error/bulk variants are separate from this bounded retained-context journey. |
| Session detail | Overview, Conversation, Events, Todos, Metrics, Context, Explorer, Timeline and nested panels | Partial | Partial | Partial | Large real session (2,822 turns / 31,666 events) used read-only across core tabs; exact nested coverage is in the per-surface matrix. UX-014 native L End→M preserves tree339/reader320; UX-015 native Enter opens Todo details and Escape returns SVG focus. Rich Explorer fixtures add native D JSONL/CSV search/no-match/reset with Unicode, invalid-JSON raw fallback, PNG zoom/Fit and binary guidance. JSON after images D/M/L inspected, though the M key lies below the viewport. These checks do not complete every detail tab/state, format or viewport. |
| Detached windows | Open, focus, tabs, close, restore; window sizing | Partial | Partial | Untested | Actual Rust Pop Out creates second WebView and leaves two main tabs. Child D Overview loads 842 events/140 turns/67.62 AIC; M Conversation 140 turns/140 tools/11m40s with Compact Space/Chat Enter and no overflow. All 8 child tabs now have bounded states; Events paging/filter/reset, emptyTodos/Timeline, Metrics/Context counts and actual491B YAML in Explorer800×500. ChildConversation/minExplorer images inspected. RepeatPopOut reuseschild and Close window Enter leavesone main; remaining populated/error/controls/L/minimumtab variants remain. |
| Search | Queries, qualifiers, help, result navigation, empty/errors, reset | Partial | Partial | Partial | Native D query → 87 matches → Newest → page 2 shows 51–87/87 with 37 links and correct Previous/Next bounds; Enter opens the matching event in a synthetic session and Back retains query/sort/page. One content-type cycle Enter off→include 73, Space→exclude 14, Enter→off resets paging; Oldest selected. M syntax Enter/Escape restores focus, repository+tool no-match and clear to 1,147 entries checked. UX-045 final D/M/L header widths equal scroll widths836/356/1956, with inspected no-outer-tab images; native Space/Enter and per-session Filter Enter→one match pass. Facet counts286before/73after differ, so no pixel-identical pair claimed. Other filters, clipboard, expanded context, errors/races and L remain partial. |
| Analytics / Tools / Code / Models | Data, filters, charts, tables, no matches, drilldown | Partial | Partial | Partial | UX-017 Analytics Custom at M shows all six presets and both date fields; original visual pair invalid, final after inspected. UX-018 native Models Enter/Space changes sort direction with visible focus and confined M table scrolling; 21 tests supplement this. UX-039 native Analytics D/M/L has compact averages and no panel/document overflow; D/M/L after images inspected. UX-040 native Code D/M/L consistently shows 1 unique path, 70 modifications and 70 sessions without overflow; D/M/L after inspected. UX-042 reversed Custom dates in Code M reproduced a backend error; paired draft correction passes 45 tests/typecheck and independent review; native M inline validation preserves prior cards, correction restores Analytics 73 sessions and dates/repository persist into Tools. Tools All Time now has native D/M/L populated checks: 286 calls, 5 tools, 99.7% success, 1.1s average and `view` tool 281 calls/1 failure, no document overflow; D/M/L images inspected; other filters/charts, normalization and persistence remain untested. |
| Compare | Session selection, compare, clear, differences | Partial | Partial | Deferred | Earlier real-session comparison and normalization pass. Final native D empty fixture→Rich shows turns 0→5, tools 0→4 and duration 0→39s as From 0. Rich→Long shows turns 5→140 =2700%, duration 39s→14m1s =2056%, files 0→1 and lines 0→420 as From 0. Both final images inspected, UX-019; fixture data differs from original before, so no exact pair. Deeper selection/reset/null/error exploration remains user-deferred. |
| Replay / Export | Feature gates, select, controls, preview, export/import | Partial | Partial | Partial | Export keyboard picker selects an owned fixture from 72 options and loads a 49,369-character Rust preview at D (UX-020); final image inspected with different data from before. UX-022 final aligned D/M headers verify Raw/Rendered selected and disabled states plus Zip guidance/no Copy. Import's named file chooser has native M focus/after image (UX-021). Later native D synthetic JSON export/load fails its own import hash check (UX-041); after the parser correction the original archive validates and displays the Rust preview at D, then Import was canceled. A successful persisted round trip and actual OS-dialog interaction remain unverified; deeper Import is user-deferred. UX-043 Replay native after has single slider steps, Home/End boundaries, 2× Space selection while paused and working Play/Pause keyboard activation; D/M/L no overflow, M/L images inspected, settled D Replay image explicitly deferred to keep Replay OFF under the latest user direction. Export presets/redaction remain partial; deeper Replay is user-deferred. Palette Replay gate defect belongs to UX-029, not verified Replay functionality. |
| Command Centre / Launcher | Status, forms, validation, plans/presets, session actions | Partial | Partial | Untested | UX-023 final settled M image is inspected: Mission Control is disabled with coming-soon guidance; Manage Worktrees Space reaches the actual route. The earlier loading image is rejected as visual proof. UX-025 settled Launcher M after shows 232px Repository field and full Branch/Model/Prompt; field labels/Advanced switch names inspected. HMR mismatch resolved by restart with Rust connected. Deeper Launcher/template execution deferred; no paid requests sent. |
| Worktrees / Config Injector | Repositories, create/remove fixtures, preview/apply/restore | Partial | Partial | Partial | Worktrees D/L 11-row after images are aligned/readable. UX-024 native M owned fixture now has a 256px branch column, seven aligned headers/cells, Space sort ascending→descending and Enter Details loads Rust data; both after images inspected. D/L real data and M fixture differ, so no matching pair. Only the newly registered fixture repository was removed from the app; its files remain. Deeper lifecycle user-deferred. Config Injector's earlier D empty/default tabs are supplemented by M populated tabs with two agents, two versions and two backups from 12 owned config-only files. UX-053/054 now passes native M names/disclosure/boolean Save/reload/unknown-field preservation and owned backup/diff workflows, with inspected after/66 tests/typecheck/independent 33 review. UX-057 restore/re-entry now returns disk/UI false with Save disabled;77 tests/typecheck/independent 41-test review pass. Config markers are not CLI execution proof; exact manifest settings.json/config.json contents are restored and hash-verified. |
| MCP | List/detail/add/edit, tools, connection states | Partial | Partial | Untested | Disposable unavailable executable: create, required-name validation, rename/save, persisted JSON, error health and no-match exercised. UX-006/007/011/012 nativeMafter verified: named Add/env/HTTP URL fields, zero unsupported controls, literal comma preview and two-argument Save/reopen/disk persistence; health284/284 tiles2×2 and Response6ms, all three after images inspected. Deeper experimental MCP transport/service/integration exploration is explicitly deferred at the user's direction; it is not treated as passed. |
| Skills | List/search/filter, editor, assets, import/export, validation, persistence | Partial | Partial | Partial | Disposable create/edit/save/import and dirty-route guards pass. UX-010 unchanged audit-check editor at M shows Saved with Save disabled; skill-saved-final.yml records the check. UX-030 readonly card, UX-037 asset keyboard/preview and UX-038 staging-backend duplicate preservation have native after evidence. UX-044 corrected duplicate-create recovery persists in actual SKILL.md. UX-055/056 editor geometry is inspected at D/M/L; keyboard resizing, correct heading/Unicode bullet and owned metadata/asset/enable/delete lifecycle pass. L list, other diagnostic/error states and GitHub import remain separate. |
| CLI Context | Saved viewer, readiness, capture and comparison | Partial | Partial | Partial | Existing snapshot viewer/comparison is read-only; UX-031 actual CLI capture after is verified D/M/L; the original route/data pair differs. Later isolated session capture at M passes CLI 1.0.83 preflight, three protocol choices/default OpenAI Responses/Save OFF and visible nine-stage progress, then fails before a model request with CLI exit1. Retry/Cancel capture Enter restores editable form; Back returns empty snapshots. Before/progress/error inspected,15 source hashes unchanged/scratch0/saved0. Later compatible-fixture native capture/save/reload/delete passes; earlier exit1 is preserved as observed failure. CLI benchmark execution and the /cli-context route at L remain separate from the session capture viewer. |
| Settings | Every section, defaults, validation, cancel/save, persistence | Partial | Partial | Partial | Shell D/M/L; theme/scale restored; names/shared Clear and 15 untoggled overrides checked. UX-032 native M invalid width restores 1600/CSS with explanation; after inspected but not matched to before. UX-033 negative Add disabled, valid 1.25 saved, invalid existing edit preserves TOML 1.25 and Escape restores it; default reset 99→5 and custom removal verified, after images inspected. UX-034 rebuild reports Indexed 74 sessions. UX-036 M Apply disables both Rebuild buttons during save, then analytics rebuild finds 74; after inspected. Reset Cancel/Escape restore trigger focus; capture deletion remains untested with zero snapshots. Warn persistence/restore and alert-event/slider basics checked; About M shows 0.8.1/build b40e4c20/74 sessions/796KB and Check Now reports latest 0.8.1, with inspected images. Two fixture alerts support keyboard read/dismiss/clear/reopen/Escape focus; M empty image settled. Other controls, actual session-alert navigation/OS delivery and recovery remain partial. |
| Global recovery | Missing routes/resources, failures, retry, disabled actions, focus | Partial | Partial | Untested | Unknown route D recovery to Sessions passes. Actual Conversation frontend SDK provider-key exception at M shows ErrorBoundary; Try Again Enter/reload repeat failure, Settings navigation works. Inspected conversation-native-boundary-D.png is misnamed: actual M960×640. This establishes frontend fallback/failed retry/navigation, not Rust recovery. Stable SDK/MCP keys are implemented; clean native restart renders both normally with zero current console errors. Full live HMR mutation and other recovery states remain unverified. |

## Validation and reconciliation

This records the final bounded native audit checkpoint; the clean full frontend retry passed 386 files / 3,497 tests at local commit 5ceece39, as recorded in validation.md. UX-001 through UX-057 are documented, with DEV-001 tracked separately. SDK UX-048/049 passes focused checks/review and native M failure/retry/accessibility plus D/L containment. UX-050/051 have bounded native after checks; UX-052 passes tests/review and native M populated-menu keyboard/semantics/focus checks after a clean restart; the bridge was disconnected and SDK switched OFF afterward. Config Injector UX-053/054 passes 66 tests/typecheck, independent 33 review and native M names/disclosure/Save/reload/backup/diff checks. UX-057 ordinary restore/re-entry now passes natively; final scoped correction passes 77 tests/typecheck and independent 41-test review. Skills UX-055/056 passes independent 55 tests/typecheck/review and inspected native D/M/L after; the owned lifecycle completes persistence/enable/asset/delete checks. UX-030/032–037 have bounded native after results, including inspected pricing, validation-bypass, maintenance-save and asset-keyboard evidence. UX-031 has bounded Explorer JSON after evidence, but actual CLI capture after D/M/L now verifies the shared fix; the original route/data pair differs. UX-039/040 have D/M/L native geometry/content after checks and inspected D/M/L images. UX-041 originally blocked importing an unchanged synthetic export; the corrected parser now validates the same original archive and shows the Rust preview at D; Import was canceled. Deeper Import/custom-table reconstruction remains explicitly deferred. UX-043 Replay keyboard correction passes 52 tests and bounded native after, with M/L images inspected and settled D Replay image explicitly deferred to keep Replay OFF under the latest user direction. UX-042 date validation passes 45 tests/typecheck and independent review; native M preserves prior cards with inline validation, corrects to Analytics 73 sessions and retains dates/repository into Tools. Matching M after inspected; physical segmented input remains untested. UX-044 skill-create feedback/names pass three tests, independent review and native M corrected create/disk/editor verification. Search now has bounded native query/paging/deep-link/Back, tri-state, syntax-focus and no-match/reset checks; UX-045 final D/M/L width/images and native collapse/per-session-filter checks pass alongside55desktop/fourChromiumtests; facetcount differences prevent pixel-identicalpairs. UX-046 Keyboard Help D/M/L after passes with all keys contained and open/Escape/editable suppression checked. UX-047 tab menu has 24 passing tests plus native focus/navigation/Escape and actual Pop Out/child data checks; Tab dismissal/Close Others/Close All, all 8 bounded child tabs and repeatPopOut/Closewindow checks also pass. Delete focus recovery, native edge placement and remaining child states remain untested. UX-038's first exclusive-create implementation passed the native duplicate/persistence check, but independent review identified lost imported-file permissions and incomplete-file recovery; the revised staging implementation passes 26 Windows Rust tests, Clippy and formatting plus native D duplicate/persistence/Open/Escape after checks. No partial family is treated as complete. Deeper MCP/Worktrees/Launcher/Import/Compare/Replay/SDK/Alerts work remains explicitly deferred rather than passed.

The earlier repository-wide validation snapshot passed `pnpm typecheck`, the full `pnpm test` retry (365 files / 3,306 tests), `pnpm build`, 1,431 non-desktop Rust tests, Clippy and formatting. A later completed full frontend run passed 375 files / 3,403 tests; subsequent targeted checks are recorded separately and are not summed into a new full-suite result. [Validation and commit checkpoint](validation.md) records package counts, the initial timing-test retry, baseline/security/tool limitations and coherent commits. Neither historical full-suite result claims to verify changes made after that run.

Recorded targeted checks also include Explorer, Todo/overlay, Search, model/delta, Export, Worktree, Launcher and shared SearchInput regressions. TabNav tests cover route/local selection, keyboard reveal, resize and unchanged ancestor scrolling; 3 Chromium JSON geometry tests cover UX-031. Component semantics, invocation and browser scroll assertions do not establish native backend or OS behavior; those checks are identified separately in the per-surface matrix.

Independent source reviews of Explorer resizing and the earlier product diff found no additional actionable regression. Subsequent review identified stale documentation claims and two UX-038 Rust follow-ups, recorded above. This checkpoint preserves exactly 124 matching inventory/matrix IDs: 97 Partial, 0 Source-only and 27 Deferred remainder. Reviews do not cover subsequent edits automatically; the latest Settings/Skills reviews are recorded separately from earlier review passes and do not erase their scope limits. Commit `278d7ea7` contains Settings validation, wizard guards and serialized maintenance. Eight add-only viewer fixtures and three generator tests preserve original files; selected JSON/JSONL/CSV/invalid JSON/PNG/binary variants now have bounded native Explorer checks. SVG has native D Plain Text406B readback consistent with Rust's unknown-extension Text fallback; SVG is not an image renderer in the current contract; other controls and remaining format/viewport combinations remain unverified. Six fixture-only images have passed root privacy review and are linked as toolbar, Analytics and Code before/after pairs in the findings. All 28 route rows have been visited; nested states and remaining workflows are still incomplete. [Draft PR #813](https://github.com/MattShelton04/TracePilot/pull/813) is open after checkpoint commit fb2ac5eb. Additional private captures are not approved for publication; unexercised workflows remain explicit audit limits and final automated validation/PR readiness is separate; transitional/mismatched/uninspected captures remain excluded as matching visual proof. See [surface-coverage.md](surface-coverage.md).

Development-only DEV-001 is recorded separately: Vite HMR import aliases split Symbol injection keys, producing a loaded retained header but empty Overview. Clean restart restored 842 events/140 turns/67.62 AIC; stable-key hardening in e42ccf64 has two failing-before regressions and 60 passing tests/typecheck. Native after confirms those Overview values and identical context keys imported through two query URLs. The after image is now root inspected with correct Overview values; this is module-identity/render verification, not a full scripted HMR mutation. It is separate from UX-048's SDK diagnostic defect and is not evidence of a packaged-app data defect.

Latest SDK discovery adds UX-048/049: at native M, diagnostics reports green Connected despite an actual unused-loopback connection failure and final error state; Advanced is a click-only DIV, the log-level select is unnamed and the URL field relies on its placeholder. Both before images were root inspected. Corrections pass 35 focused tests, an independent 23-test review, typecheck/Biome and native M failure/retry/disclosure/field-name checks with inspected after evidence; before/after state/scroll/timestamps differ. SRC-P11/P12 have bounded native failure/accessibility observations. No inference was sent. UX-012 now has native complex-argument after: readonly field/associated guidance, Description Save Enter and exact two-argument Rust JSON preservation. The final CR-array check and associated 12px helper also pass with inspected evidence; the full original MCP backup hash is restored and common arguments reload editable.

Native M Explorer search covers name/content query, actual café line-5 highlighting and no-match/Clear/Escape. UX-050 now passes native M initial Copy File Path focus, End/Home navigation and Escape exact-file focus restoration; its menu ends at632 inside640. Before/after images are inspected but background/retained-tab differences prevent an exact pair. Combined 26 file/tab/model-picker tests and bounded independent review pass; D/L placement/Tab focus is now verified; directory/action variants remain. No clipboard/external-folder action was invoked. C10/C11 now have native M preflight/progress/error/cancel observations; no Source-only rows remain. G10 has actual frontend exception evidence. Config Injector M populated workflows and UX-053/054 after supplement earlier empty/default checks; UX-057 ordinary/route re-entry after passes, with final77 tests/typecheck/independent 41-test review.

SDK UX-048/049 native M failure/retry/disclosure/names and D/L control containment pass; inspected screenshots containing account details remain private. Commit e253e858 contains the correction. UX-052's shared menu correction passes bounded tests/review and native M after a clean restart: 15 menuitemradios, End to Grok 4.6, Home to Default, Escape closes and restores the exact default-model trigger. The populated after image is inspected. SDK/MCP stable keys and clean native rendering now pass with zero current console errors; the earlier DEV-001 interruption remains historical frontend evidence. SDK/Replay/Alerts are OFF by user direction and deeper work is deferred. Native D SQLite UX-051 after passes Enter/full Unicode/Escape exact-cell restoration, Home/Right cell navigation, note resize180→196/Enter180 and one data tab stop. Its inspected after differs in retained tabs from before; M/L keyboard/containment now also passes; remaining value/table/copy variants and final overall review are separate. SVG Plain Text406B matches Rust's unknown-extension Text fallback; SVG image rendering is outside the current contract, so no defect is asserted.

Skills UX-055/056 now passes independent 55 tests/typecheck/Biome and native after. M panes stay at least300px with 415px right in the measured minimum state; Home/Right/End/Enter works. D600/595px and L1160/1155px have no outer overflow. Correct H1/Unicode bullet, Ctrl+S/disk/reload and metadata hint/tools/automatic OFF persistence pass; after images are inspected. The owned0B reference-café.md has a settled empty preview/Escape exact focus, then Remove Cancel/confirm deletes it. Owned skill disable/reload/reenable restores disabledSkills[]; no-match/reset/Project empty/All and Delete Cancel/Escape/confirm pass, leaving five globals. The audit-lifecycle-disposable skill is deleted; existing user skills remain untouched. Native undo/physical IME and unrecorded error variants remain.

UX-050 now also passes D/L keyboard/pointer menu placement and Tab/exact-file focus with inspected images; binary M Copy Path/Open Folder/Escape passes; directory/actions remain. SQLite UX-051 passes M/L full Unicode Enter/Escape, Ctrl+End row120last/Ctrl+Home row1first, resize196→180, one data tab stop and no outer overflow; images inspected, commit22a02025. Native ordinary UX-057 restore passes disk/UI false, Save disabled/diff0 with inspected after. Independent remount regressions then exposed two races, now corrected with 77 passing Config tests/typecheck; native ordinary/route re-entry is verified; final Restore-only guard passes independent 41-test review. The owned audit-restore-verification backup was deleted through two-step UI; baseline two backups remain.

Capture now has native failure/cancel and successful end-to-end coverage. A distinct compatible CLI 1.0.83 fixture produces68.1KB/7items/16tools/1system; Overview/System/Items/Tools/Raw keyboard tabs pass. View once Back returns empty; Save ON recapture survives reload/open, then Delete Cancel/Escape and confirm return empty/disk0. Actual long CLI JSON now verifies UX-031 at D/M/L with inspected images; the original saved-snapshot route/data pair differs. Save OFF is restored,15 source hashes unchanged/scratch0/saved0, and only the owner/path-checked temporary folder was removed. Original rich data stays intact.

Later Settings checks verify Hide empty73→74→73, persisted Markdown/Rich Rendering OFF raw Chat/tool content then ON rendering, D Light125% then Dark100% restoration, and L24-card repository/long-branch/sort context retained through Conversation and Back. Relevant images are inspected. Agent model autosave/reload and restoration pass; original settings.json/config.json manifest contents are hash-restored and Alerts diskfalse is verified. Full frontend3,491 tests/385 files, five-package typecheck and build15.35s passed before the final UX-057 follow-up; its affected77-test group/typecheck is separate. Skills commit9c757bd1, menus96a7399c and SQLite22a02025 are local checkpoints, not claims of newer hosted CI.

## Final native reconciliation and limits

All 28 routes have native visits and all 124 source families have an explicit disposition: 97 Partial and 27 Deferred remainder, with zero Source-only rows. Partial means the recorded subset was exercised; it does not claim every interaction or viewport/state combination. The 57 UX findings have implemented corrections with their attributed native and automated evidence; DEV-001 remains separate development-runtime evidence.

UX-004 now passes in a fresh empty Tauri profile at D/M/L, including M Settings navigation and reload; restoring the populated app preserves closed config/index/registry hashes and real Rust connectivity. UX-019 final native D zero-baseline and whole-percent rounding images now pass on owned fixtures; the original real-data pair remains unmatched. UX-043's settled D image is deferred to keep Replay OFF. The native subsets and 20/52 focused tests respectively remain accepted. Other missing matching pairs and unexercised nested/error/OS/DPI/IME variants remain plainly listed in the detailed matrix; six fixture-only public images are approved and all other captures stay private.

Final local validation passes at 5ceece39: the clean full frontend retry reports 386 files / 3,497 tests. The preceding single context-import timeout is preserved as a failed attempt; the test-only correction keeps the same assertions/timeout and passes independent review. Workspace typecheck, latest desktop typecheck, production build and file-size policy passes are scoped in [validation](validation.md). The native app is restored to populated Sessions at D, Dark/100%, 73 visible items/empty search, SDK/Replay/Alerts OFF; its current console has zero errors and three warnings. Hosted results at fb2ac5eb remain historical until a newer push/run is recorded.
